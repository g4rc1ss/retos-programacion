{
  "name": "reto-06-piedra-papel-tijera-lagarto-y-spock",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm install && npm run tsc",
    "tsc": "tsc",
    "dev": "ts-node-dev --respawn --transpile-only ./index.ts",
    "prod": "tsc && node ./build/index.js",
    "reinstall": "npm run clean && npm install",
    "clean": "rimraf ./node_modules ./build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "rimraf": "^5.0.1",
    "typescript": "^5.1.6"
  },
  "devDependencies": {
    "@types/node": "^20.4.6",
    "ts-node-dev": "^2.0.0"
  }
}
